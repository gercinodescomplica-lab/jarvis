#!/bin/sh
git diff --cached --name-only --diff-filter=ACM | grep -qE '\.(ts|tsx|js|jsx)$' || exit 0
echo "🔍 Biome lint..."
npx biome lint . || { echo "❌ Corrija os erros antes de commitar."; exit 1; }
